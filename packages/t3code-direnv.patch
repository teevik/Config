# t3code-base-version: 0.0.39-nightly.20260903.1268
diff --git a/apps/server/src/provider/Drivers/ClaudeDriver.ts b/apps/server/src/provider/Drivers/ClaudeDriver.ts
index d47d9c062ae2ce49c4b1306392a278229a2ffbcb..881610bbc6aa906aed637480ac0141c1c8e5742e 100644
--- a/apps/server/src/provider/Drivers/ClaudeDriver.ts
+++ b/apps/server/src/provider/Drivers/ClaudeDriver.ts
@@ -44,7 +44,10 @@ import {
   type ProviderInstance,
 } from "../ProviderDriver.ts";
 import { withInstanceIdentity } from "./instanceIdentity.ts";
-import { mergeProviderInstanceEnvironment } from "../ProviderInstanceEnvironment.ts";
+import {
+  makeProviderEnvironmentResolver,
+  mergeProviderInstanceEnvironment,
+} from "../ProviderInstanceEnvironment.ts";
 import {
   enrichProviderSnapshotWithVersionAdvisory,
   makePackageManagedProviderMaintenanceResolver,
@@ -116,6 +119,7 @@ export const ClaudeDriver: ProviderDriver<ClaudeSettings, ClaudeDriverEnv> = {
       const modelManifest = yield* ModelManifest.ModelManifest;
       const modelCatalog = modelManifest.current.pipe(Effect.map(resolveClaudeModelCatalog));
       const processEnv = mergeProviderInstanceEnvironment(environment);
+      const resolveEnvironment = makeProviderEnvironmentResolver(environment);
       const fallbackContinuationIdentity = defaultProviderContinuationIdentity({
         driverKind: DRIVER_KIND,
         instanceId,
@@ -136,6 +140,7 @@ export const ClaudeDriver: ProviderDriver<ClaudeSettings, ClaudeDriverEnv> = {

       const adapterOptions = {
         instanceId,
+        resolveEnvironment,
         environment: processEnv,
         modelCatalog,
         ...(eventLoggers.native ? { nativeEventLogger: eventLoggers.native } : {}),
diff --git a/apps/server/src/provider/Drivers/CodexDriver.ts b/apps/server/src/provider/Drivers/CodexDriver.ts
index e9a75780a544d1e424b4721af31c5bade3fb84b6..58d9d83b1c10c6ccc522fd1459fb4db5d7319efa 100644
--- a/apps/server/src/provider/Drivers/CodexDriver.ts
+++ b/apps/server/src/provider/Drivers/CodexDriver.ts
@@ -47,7 +47,10 @@ import { makeManagedServerProvider } from "../makeManagedServerProvider.ts";
 import * as ModelManifest from "../ModelManifest.ts";
 import type { ProviderDriver, ProviderInstance } from "../ProviderDriver.ts";
 import { withInstanceIdentity } from "./instanceIdentity.ts";
-import { mergeProviderInstanceEnvironment } from "../ProviderInstanceEnvironment.ts";
+import {
+  makeProviderEnvironmentResolver,
+  mergeProviderInstanceEnvironment,
+} from "../ProviderInstanceEnvironment.ts";
 import {
   enrichProviderSnapshotWithVersionAdvisory,
   makePackageManagedProviderMaintenanceResolver,
@@ -106,6 +109,7 @@ export const CodexDriver: ProviderDriver<CodexSettings, CodexDriverEnv> = {
       const eventLoggers = yield* ProviderEventLoggers;
       const modelManifest = yield* ModelManifest.ModelManifest;
       const processEnv = mergeProviderInstanceEnvironment(environment);
+      const resolveEnvironment = makeProviderEnvironmentResolver(environment);
       const homeLayout = yield* resolveCodexHomeLayout(config);
       const continuationIdentity = codexContinuationIdentity(homeLayout);
       const stampIdentity = withInstanceIdentity({
@@ -145,6 +149,7 @@ export const CodexDriver: ProviderDriver<CodexSettings, CodexDriverEnv> = {
       const adapter = yield* makeCodexAdapter(effectiveConfig, {
         instanceId,
         environment: processEnv,
+        resolveEnvironment,
         ...(eventLoggers.native ? { nativeEventLogger: eventLoggers.native } : {}),
       });
       const textGeneration = yield* makeCodexTextGeneration(effectiveConfig, processEnv);
diff --git a/apps/server/src/provider/Drivers/CursorDriver.ts b/apps/server/src/provider/Drivers/CursorDriver.ts
index 9dbdaaaf7c8f3b564dfbc699f74b54aa67060ed1..2e42693f98f3659f110745adf9de5f1070e795dc 100644
--- a/apps/server/src/provider/Drivers/CursorDriver.ts
+++ b/apps/server/src/provider/Drivers/CursorDriver.ts
@@ -39,7 +39,10 @@ import {
   type ProviderInstance,
 } from "../ProviderDriver.ts";
 import { withInstanceIdentity } from "./instanceIdentity.ts";
-import { mergeProviderInstanceEnvironment } from "../ProviderInstanceEnvironment.ts";
+import {
+  makeProviderEnvironmentResolver,
+  mergeProviderInstanceEnvironment,
+} from "../ProviderInstanceEnvironment.ts";
 import {
   makeProviderMaintenanceCapabilities,
   type ProviderMaintenanceCapabilitiesResolver,
@@ -94,6 +97,7 @@ export const CursorDriver: ProviderDriver<CursorSettings, CursorDriverEnv> = {
       const serverSettings = yield* ServerSettingsService;
       const eventLoggers = yield* ProviderEventLoggers;
       const processEnv = mergeProviderInstanceEnvironment(environment);
+      const resolveEnvironment = makeProviderEnvironmentResolver(environment);
       const continuationIdentity = defaultProviderContinuationIdentity({
         driverKind: DRIVER_KIND,
         instanceId,
@@ -113,6 +117,7 @@ export const CursorDriver: ProviderDriver<CursorSettings, CursorDriverEnv> = {

       const adapter = yield* makeCursorAdapter(effectiveConfig, {
         environment: processEnv,
+        resolveEnvironment,
         ...(eventLoggers.native ? { nativeEventLogger: eventLoggers.native } : {}),
         instanceId,
       });
diff --git a/apps/server/src/provider/Drivers/GrokDriver.ts b/apps/server/src/provider/Drivers/GrokDriver.ts
index 8957b4c677816b07abf7759caf2fdd93d56d8e6f..98dff4977926d55093be742585bddcefab18489e 100644
--- a/apps/server/src/provider/Drivers/GrokDriver.ts
+++ b/apps/server/src/provider/Drivers/GrokDriver.ts
@@ -26,7 +26,10 @@ import {
   type ProviderInstance,
 } from "../ProviderDriver.ts";
 import { withInstanceIdentity } from "./instanceIdentity.ts";
-import { mergeProviderInstanceEnvironment } from "../ProviderInstanceEnvironment.ts";
+import {
+  makeProviderEnvironmentResolver,
+  mergeProviderInstanceEnvironment,
+} from "../ProviderInstanceEnvironment.ts";
 import { discoverGrokSkills } from "./GrokSkills.ts";
 import {
   makeManualOnlyProviderMaintenanceCapabilities,
@@ -76,6 +79,7 @@ export const GrokDriver: ProviderDriver<GrokSettings, GrokDriverEnv> = {
       const { cwd } = yield* ServerConfig;
       const eventLoggers = yield* ProviderEventLoggers;
       const processEnv = mergeProviderInstanceEnvironment(environment);
+      const resolveEnvironment = makeProviderEnvironmentResolver(environment);
       const continuationIdentity = defaultProviderContinuationIdentity({
         driverKind: DRIVER_KIND,
         instanceId,
@@ -95,6 +99,7 @@ export const GrokDriver: ProviderDriver<GrokSettings, GrokDriverEnv> = {

       const adapter = yield* makeGrokAdapter(effectiveConfig, {
         environment: processEnv,
+        resolveEnvironment,
         ...(eventLoggers.native ? { nativeEventLogger: eventLoggers.native } : {}),
         instanceId,
       });
diff --git a/apps/server/src/provider/Drivers/OpenCodeDriver.ts b/apps/server/src/provider/Drivers/OpenCodeDriver.ts
index 8ff01946644ce246a0d62e1be6aa69d30dd7dde9..4f601b90b40f111958efc592ab63d67e5ac60a1f 100644
--- a/apps/server/src/provider/Drivers/OpenCodeDriver.ts
+++ b/apps/server/src/provider/Drivers/OpenCodeDriver.ts
@@ -42,7 +42,10 @@ import {
   type ProviderInstance,
 } from "../ProviderDriver.ts";
 import { withInstanceIdentity } from "./instanceIdentity.ts";
-import { mergeProviderInstanceEnvironment } from "../ProviderInstanceEnvironment.ts";
+import {
+  makeProviderEnvironmentResolver,
+  mergeProviderInstanceEnvironment,
+} from "../ProviderInstanceEnvironment.ts";
 import {
   enrichProviderSnapshotWithVersionAdvisory,
   makePackageManagedProviderMaintenanceResolver,
@@ -106,6 +109,7 @@ export const OpenCodeDriver: ProviderDriver<OpenCodeSettings, OpenCodeDriverEnv>
       const serverSettings = yield* ServerSettingsService;
       const eventLoggers = yield* ProviderEventLoggers;
       const processEnv = mergeProviderInstanceEnvironment(environment);
+      const resolveEnvironment = makeProviderEnvironmentResolver(environment);
       const continuationIdentity = defaultProviderContinuationIdentity({
         driverKind: DRIVER_KIND,
         instanceId,
@@ -126,6 +130,7 @@ export const OpenCodeDriver: ProviderDriver<OpenCodeSettings, OpenCodeDriverEnv>
       const adapter = yield* makeOpenCodeAdapter(effectiveConfig, {
         instanceId,
         environment: processEnv,
+        resolveEnvironment,
         ...(eventLoggers.native ? { nativeEventLogger: eventLoggers.native } : {}),
       });
       const serverOwner = yield* OpenCodeServerOwner.make({
diff --git a/apps/server/src/provider/Layers/ClaudeAdapter.ts b/apps/server/src/provider/Layers/ClaudeAdapter.ts
index 62763f947c7d725d2d855e9403843b0525fb3e54..da5c1511daf7c56177af80c0597813b8d9d412fe 100644
--- a/apps/server/src/provider/Layers/ClaudeAdapter.ts
+++ b/apps/server/src/provider/Layers/ClaudeAdapter.ts
@@ -335,6 +335,7 @@ interface ClaudeQueryRuntime extends AsyncIterable<SDKMessage> {
 export interface ClaudeAdapterLiveOptions {
   readonly instanceId?: ProviderInstanceId;
   readonly environment?: NodeJS.ProcessEnv;
+  readonly resolveEnvironment?: import("../ProviderInstanceEnvironment.ts").ProviderEnvironmentResolver;
   readonly createQuery?: (input: {
     readonly prompt: AsyncIterable<SDKUserMessage>;
     readonly options: ClaudeQueryOptions;
@@ -1730,13 +1731,6 @@ export const makeClaudeAdapter = Effect.fn("makeClaudeAdapter")(function* (
   const path = yield* Path.Path;
   const serverConfig = yield* ServerConfig;
   const crypto = yield* Crypto.Crypto;
-  const claudeEnvironment = yield* makeClaudeEnvironment(claudeSettings, options?.environment).pipe(
-    Effect.provideService(Path.Path, path),
-  );
-  const claudeSdkExecutablePath = yield* resolveClaudeSdkExecutablePath(
-    claudeSettings.binaryPath,
-    claudeEnvironment,
-  );
   const nativeEventLogger =
     options?.nativeEventLogger ??
     (options?.nativeEventLogPath !== undefined
@@ -3863,6 +3857,17 @@ export const makeClaudeAdapter = Effect.fn("makeClaudeAdapter")(function* (
         });
       }

+      const environment = options?.resolveEnvironment
+        ? yield* options.resolveEnvironment(input.cwd ?? serverConfig.cwd)
+        : options?.environment;
+      const claudeEnvironment = yield* makeClaudeEnvironment(claudeSettings, environment).pipe(
+        Effect.provideService(Path.Path, path),
+      );
+      const claudeSdkExecutablePath = yield* resolveClaudeSdkExecutablePath(
+        claudeSettings.binaryPath,
+        claudeEnvironment,
+      );
+
       const existingContext = sessions.get(input.threadId);
       if (existingContext) {
         yield* Effect.logWarning("claude.session.replacing", {
@@ -4653,6 +4658,13 @@ export const makeClaudeAdapter = Effect.fn("makeClaudeAdapter")(function* (
       });
     }

+    const environment = options?.resolveEnvironment
+      ? yield* options.resolveEnvironment(context.session.cwd)
+      : options?.environment;
+    const claudeEnvironment = yield* makeClaudeEnvironment(claudeSettings, environment).pipe(
+      Effect.provideService(Path.Path, path),
+    );
+
     // Re-scan on every send: skills are added and switched off mid-session,
     // and the scan is a few directory reads. A skill switched off via
     // skillOverrides, or reserved for the agent with `user-invocable: false`,
diff --git a/apps/server/src/provider/Layers/CodexAdapter.test.ts b/apps/server/src/provider/Layers/CodexAdapter.test.ts
index bcaad3cb5e57b356b5358dac68e6bac2ba30b366..d0b12b614a64a3bf6471667b495a418e74db1b4f 100644
--- a/apps/server/src/provider/Layers/CodexAdapter.test.ts
+++ b/apps/server/src/provider/Layers/CodexAdapter.test.ts
@@ -466,6 +466,47 @@ sessionErrorLayer("CodexAdapterLive session errors", (it) => {
     }).pipe(Effect.provide(layer));
   });

+  it.effect("resolves the project environment for the session cwd", () => {
+    const runtimeFactory = makeRuntimeFactory();
+    const resolvedCwds: string[] = [];
+    const layer = Layer.effect(
+      CodexAdapter,
+      Effect.gen(function* () {
+        const codexConfig = decodeCodexSettings({});
+        return yield* makeCodexAdapter(codexConfig, {
+          resolveEnvironment: (cwd) => {
+            resolvedCwds.push(cwd);
+            return Effect.succeed({ PATH: "/project/bin", PROJECT_MARKER: "loaded" });
+          },
+          makeRuntime: runtimeFactory.factory,
+        });
+      }),
+    ).pipe(
+      Layer.provideMerge(ServerConfig.layerTest(process.cwd(), process.cwd())),
+      Layer.provideMerge(ServerSettingsService.layerTest()),
+      Layer.provideMerge(providerSessionDirectoryTestLayer),
+      Layer.provideMerge(NodeServices.layer),
+    );
+
+    return Effect.gen(function* () {
+      const adapter = yield* CodexAdapter;
+      yield* adapter.startSession({
+        provider: ProviderDriverKind.make("codex"),
+        threadId: asThreadId("sess-project-environment"),
+        cwd: "/workspace/project",
+        runtimeMode: "full-access",
+      });
+
+      const runtime = runtimeFactory.lastRuntime;
+      NodeAssert.ok(runtime);
+      NodeAssert.deepStrictEqual(resolvedCwds, ["/workspace/project"]);
+      NodeAssert.deepStrictEqual(runtime.options.environment, {
+        PATH: "/project/bin",
+        PROJECT_MARKER: "loaded",
+      });
+    }).pipe(Effect.provide(layer));
+  });
+
   it.effect("maps codex model options for the adapter's bound custom instance id", () => {
     const customInstanceId = ProviderInstanceId.make("codex_personal");
     const customRuntimeFactory = makeRuntimeFactory();
diff --git a/apps/server/src/provider/Layers/CodexAdapter.ts b/apps/server/src/provider/Layers/CodexAdapter.ts
index fa8511ee09a17a675253e2bd7f36de2fe144ab1e..ae2769a9e9387d9400c12234a5562f5ea4898346 100644
--- a/apps/server/src/provider/Layers/CodexAdapter.ts
+++ b/apps/server/src/provider/Layers/CodexAdapter.ts
@@ -69,6 +69,7 @@ import {
 } from "./CodexSessionRuntime.ts";
 import { type EventNdjsonLogger, makeEventNdjsonLogger } from "./EventNdjsonLogger.ts";
 import { resolveCodexLaunchArgs } from "./codexLaunchArgs.ts";
+import type { ProviderEnvironmentResolver } from "../ProviderInstanceEnvironment.ts";
 const isCodexAppServerProcessExitedError = Schema.is(CodexErrors.CodexAppServerProcessExitedError);
 const isCodexAppServerTransportError = Schema.is(CodexErrors.CodexAppServerTransportError);
 const isCodexSessionRuntimeThreadIdMissingError = Schema.is(
@@ -81,6 +82,7 @@ const PROVIDER = ProviderDriverKind.make("codex");
 export interface CodexAdapterLiveOptions {
   readonly instanceId?: ProviderInstanceId;
   readonly environment?: NodeJS.ProcessEnv;
+  readonly resolveEnvironment?: ProviderEnvironmentResolver;
   readonly makeRuntime?: (
     options: CodexSessionRuntimeOptions,
   ) => Effect.Effect<
@@ -1995,13 +1997,17 @@ export const makeCodexAdapter = Effect.fn("makeCodexAdapter")(function* (
             ? getCodexServiceTierOptionValue(input.modelSelection)
             : undefined;
         const mcpSession = McpProviderSession.readMcpProviderSession(input.threadId);
+        const cwd = input.cwd ?? process.cwd();
+        const environment = options?.resolveEnvironment
+          ? yield* options.resolveEnvironment(cwd)
+          : options?.environment;
         const runtimeInput: CodexSessionRuntimeOptions = {
           threadId: input.threadId,
           providerInstanceId: boundInstanceId,
-          cwd: input.cwd ?? process.cwd(),
+          cwd,
           binaryPath: codexConfig.binaryPath,
-          launchArgs: resolveCodexLaunchArgs(codexConfig.launchArgs, options?.environment),
-          ...(options?.environment ? { environment: options.environment } : {}),
+          launchArgs: resolveCodexLaunchArgs(codexConfig.launchArgs, environment),
+          ...(environment ? { environment } : {}),
           ...(codexConfig.homePath ? { homePath: codexConfig.homePath } : {}),
           ...(isCodexResumeCursorSchema(input.resumeCursor)
             ? { resumeCursor: input.resumeCursor }
@@ -2014,7 +2020,7 @@ export const makeCodexAdapter = Effect.fn("makeCodexAdapter")(function* (
           ...(mcpSession
             ? {
                 environment: {
-                  ...(options?.environment ?? process.env),
+                  ...(environment ?? process.env),
                   T3_MCP_BEARER_TOKEN: mcpSession.authorizationHeader.replace(/^Bearer\s+/, ""),
                 },
                 appServerArgs: [
diff --git a/apps/server/src/provider/Layers/CursorAdapter.ts b/apps/server/src/provider/Layers/CursorAdapter.ts
index 50a3131ff9500304818a8d8837dcd64c389e9f22..5550d97abbf974da763d8277d62f20f111100629 100644
--- a/apps/server/src/provider/Layers/CursorAdapter.ts
+++ b/apps/server/src/provider/Layers/CursorAdapter.ts
@@ -77,6 +77,7 @@ import {
 import { type CursorAdapterShape } from "../Services/CursorAdapter.ts";
 import { resolveCursorAcpBaseModelId } from "./CursorProvider.ts";
 import { type EventNdjsonLogger, makeEventNdjsonLogger } from "./EventNdjsonLogger.ts";
+import type { ProviderEnvironmentResolver } from "../ProviderInstanceEnvironment.ts";
 import {
   discoverCursorSkills,
   hasCursorSkillMention,
@@ -97,6 +98,7 @@ function encodeJsonStringForDiagnostics(input: unknown): string | undefined {

 export interface CursorAdapterLiveOptions {
   readonly environment?: NodeJS.ProcessEnv;
+  readonly resolveEnvironment?: ProviderEnvironmentResolver;
   readonly nativeEventLogPath?: string;
   readonly nativeEventLogger?: EventNdjsonLogger;
   /**
@@ -502,6 +504,9 @@ export function makeCursorAdapter(
           }

           const cwd = path.resolve(input.cwd.trim());
+          const environment = options?.resolveEnvironment
+            ? yield* options.resolveEnvironment(cwd)
+            : options?.environment;
           const cursorModelSelection =
             input.modelSelection?.instanceId === boundInstanceId ? input.modelSelection : undefined;
           const existing = sessions.get(input.threadId);
@@ -540,7 +545,7 @@ export function makeCursorAdapter(
           const mcpSession = McpProviderSession.readMcpProviderSession(input.threadId);
           const acp = yield* makeCursorAcpRuntime({
             cursorSettings: effectiveCursorSettings,
-            ...(options?.environment ? { environment: options.environment } : {}),
+            ...(environment ? { environment } : {}),
             childProcessSpawner,
             cwd,
             runtimeMode: input.runtimeMode,
diff --git a/apps/server/src/provider/Layers/GrokAdapter.ts b/apps/server/src/provider/Layers/GrokAdapter.ts
index da9bdf6030f430315e367f2d01537efe42b90885..db70cd45787326f997087cc5eeedebbb2a00cad5 100644
--- a/apps/server/src/provider/Layers/GrokAdapter.ts
+++ b/apps/server/src/provider/Layers/GrokAdapter.ts
@@ -80,6 +80,7 @@ import {
 } from "../acp/XAiAcpExtension.ts";
 import { type GrokAdapterShape } from "../Services/GrokAdapter.ts";
 import { type EventNdjsonLogger, makeEventNdjsonLogger } from "./EventNdjsonLogger.ts";
+import type { ProviderEnvironmentResolver } from "../ProviderInstanceEnvironment.ts";

 const encodeUnknownJsonStringExit = Schema.encodeUnknownExit(Schema.fromJsonString(Schema.Unknown));

@@ -102,6 +103,7 @@ function encodeJsonStringForDiagnostics(input: unknown): string | undefined {

 export interface GrokAdapterLiveOptions {
   readonly environment?: NodeJS.ProcessEnv;
+  readonly resolveEnvironment?: ProviderEnvironmentResolver;
   readonly nativeEventLogPath?: string;
   readonly nativeEventLogger?: EventNdjsonLogger;
   readonly instanceId?: ProviderInstanceId;
@@ -962,6 +964,9 @@ export function makeGrokAdapter(grokSettings: GrokSettings, options?: GrokAdapte
           }

           const cwd = path.resolve(input.cwd.trim());
+          const environment = options?.resolveEnvironment
+            ? yield* options.resolveEnvironment(cwd)
+            : options?.environment;
           const grokModelSelection =
             input.modelSelection?.instanceId === boundInstanceId ? input.modelSelection : undefined;
           const existing = sessions.get(input.threadId);
@@ -988,7 +993,7 @@ export function makeGrokAdapter(grokSettings: GrokSettings, options?: GrokAdapte
           const mcpSession = McpProviderSession.readMcpProviderSession(input.threadId);
           const acp = yield* makeGrokAcpRuntime({
             grokSettings,
-            ...(options?.environment ? { environment: options.environment } : {}),
+            ...(environment ? { environment } : {}),
             childProcessSpawner,
             cwd,
             runtimeMode: input.runtimeMode,
diff --git a/apps/server/src/provider/Layers/OpenCodeAdapter.ts b/apps/server/src/provider/Layers/OpenCodeAdapter.ts
index d0b4f0de78ce1a207c67d3687199dd493d6349cb..d708a6280860d33603bc0bda79df0457164290d6 100644
--- a/apps/server/src/provider/Layers/OpenCodeAdapter.ts
+++ b/apps/server/src/provider/Layers/OpenCodeAdapter.ts
@@ -370,6 +370,7 @@ interface OpenCodeSessionContext {
 export interface OpenCodeAdapterLiveOptions {
   readonly instanceId?: ProviderInstanceId;
   readonly environment?: NodeJS.ProcessEnv;
+  readonly resolveEnvironment?: import("../ProviderInstanceEnvironment.ts").ProviderEnvironmentResolver;
   readonly nativeEventLogPath?: string;
   readonly nativeEventLogger?: EventNdjsonLogger;
 }
@@ -2380,6 +2381,9 @@ export function makeOpenCodeAdapter(
         const serverUrl = openCodeSettings.serverUrl;
         const serverPassword = openCodeSettings.serverPassword;
         const directory = input.cwd ?? serverConfig.cwd;
+        const environment = options?.resolveEnvironment
+          ? yield* options.resolveEnvironment(directory)
+          : options?.environment;
         const resumeSessionId = parseOpenCodeResume(input.resumeCursor)?.sessionId;
         const existing = sessions.get(input.threadId);
         if (existing) {
@@ -2402,7 +2406,7 @@ export function makeOpenCodeAdapter(
                 directory,
                 serverUrl,
                 ...(serverPassword ? { serverPassword } : {}),
-                ...(options?.environment ? { environment: options.environment } : {}),
+                ...(environment ? { environment } : {}),
               });
               const client = openCodeRuntime.createOpenCodeSdkClient({
                 baseUrl: server.url,
diff --git a/apps/server/src/provider/ProviderInstanceEnvironment.test.ts b/apps/server/src/provider/ProviderInstanceEnvironment.test.ts
index 7ac3f2f2837dea121216957cbf3e15cb10822966..36c6d581b4f4df0dac6fef41ae902872f788c079 100644
--- a/apps/server/src/provider/ProviderInstanceEnvironment.test.ts
+++ b/apps/server/src/provider/ProviderInstanceEnvironment.test.ts
@@ -1,10 +1,17 @@
-import { describe, expect, it } from "vite-plus/test";
+import { assert, describe, it } from "@effect/vitest";
+import * as Effect from "effect/Effect";
+import * as NodeFS from "node:fs";
+import * as NodeOS from "node:os";
+import * as NodePath from "node:path";

-import { mergeProviderInstanceEnvironment } from "./ProviderInstanceEnvironment.ts";
+import {
+  makeProviderEnvironmentResolver,
+  mergeProviderInstanceEnvironment,
+} from "./ProviderInstanceEnvironment.ts";

 describe("mergeProviderInstanceEnvironment", () => {
   it("overrides inherited environment values and preserves empty strings", () => {
-    expect(
+    assert.deepStrictEqual(
       mergeProviderInstanceEnvironment(
         [
           { name: "OPENROUTER_API_KEY", value: "sk-or-test", sensitive: true },
@@ -12,10 +19,80 @@ describe("mergeProviderInstanceEnvironment", () => {
         ],
         { ANTHROPIC_API_KEY: "inherited", PATH: "/bin" },
       ),
-    ).toMatchObject({
-      OPENROUTER_API_KEY: "sk-or-test",
-      ANTHROPIC_API_KEY: "",
-      PATH: "/bin",
-    });
+      {
+        OPENROUTER_API_KEY: "sk-or-test",
+        ANTHROPIC_API_KEY: "",
+        PATH: "/bin",
+      },
+    );
   });
 });
+
+describe("makeProviderEnvironmentResolver", () => {
+  it.effect("captures an interactive shell environment in the project cwd", () =>
+    Effect.gen(function* () {
+      const fixture = NodeFS.mkdtempSync(NodePath.join(NodeOS.tmpdir(), "t3-project-shell-"));
+      yield* Effect.addFinalizer(() =>
+        Effect.sync(() => NodeFS.rmSync(fixture, { recursive: true, force: true })),
+      );
+      const workspace = NodePath.join(fixture, "workspace");
+      const shell = NodePath.join(fixture, "project-shell");
+      const hostShell = process.env.SHELL;
+      assert.ok(hostShell);
+      NodeFS.mkdirSync(workspace);
+      NodeFS.writeFileSync(
+        shell,
+        [
+          `#!${hostShell}`,
+          'export PROJECT_MARKER="loaded"',
+          'export PROJECT_CWD="$PWD"',
+          `exec ${hostShell}`,
+        ].join("\n"),
+      );
+      NodeFS.chmodSync(shell, 0o755);
+
+      const resolve = makeProviderEnvironmentResolver(undefined, {
+        ...process.env,
+        SHELL: shell,
+      });
+      const environment = yield* resolve(workspace);
+
+      assert.strictEqual(environment.PROJECT_MARKER, "loaded");
+      assert.strictEqual(environment.PROJECT_CWD, workspace);
+    }),
+  );
+
+  it.effect("loads the cwd environment and applies provider overrides last", () =>
+    Effect.gen(function* () {
+      let receivedCwd: string | undefined;
+      const resolve = makeProviderEnvironmentResolver(
+        [{ name: "API_KEY", value: "configured", sensitive: true }],
+        { API_KEY: "inherited", PATH: "/bin", REMOVE_ME: "yes" },
+        (cwd) => {
+          receivedCwd = cwd;
+          return Effect.succeed({ API_KEY: "project", PATH: "/project/bin" });
+        },
+      );
+
+      const environment = yield* resolve("/workspace/project");
+
+      assert.strictEqual(receivedCwd, "/workspace/project");
+      assert.deepStrictEqual(environment, { API_KEY: "configured", PATH: "/project/bin" });
+    }),
+  );
+
+  it.effect("falls back to the inherited environment when shell activation fails", () =>
+    Effect.gen(function* () {
+      const resolve = makeProviderEnvironmentResolver(
+        [{ name: "API_KEY", value: "configured", sensitive: true }],
+        { PATH: "/bin" },
+        () => Effect.fail(new Error("shell activation unavailable")),
+      );
+
+      assert.deepStrictEqual(yield* resolve("/workspace/project"), {
+        API_KEY: "configured",
+        PATH: "/bin",
+      });
+    }),
+  );
+});
diff --git a/apps/server/src/provider/ProviderInstanceEnvironment.ts b/apps/server/src/provider/ProviderInstanceEnvironment.ts
index e469253604e6b5714010dae11cdee02944cc5878..eaa865f2c3fd5b6e06f4dc2247ec58f0269d9f30 100644
--- a/apps/server/src/provider/ProviderInstanceEnvironment.ts
+++ b/apps/server/src/provider/ProviderInstanceEnvironment.ts
@@ -1,4 +1,121 @@
 import type { ProviderInstanceEnvironment } from "@t3tools/contracts";
+import { HostProcessPlatform } from "@t3tools/shared/hostProcess";
+import { listLoginShellCandidates } from "@t3tools/shared/shell";
+import * as NodeChildProcess from "node:child_process";
+import * as Effect from "effect/Effect";
+
+export type ProviderEnvironmentResolver = (cwd: string) => Effect.Effect<NodeJS.ProcessEnv, never>;
+
+type ProjectShellEnvironment = Record<string, string>;
+
+const ENVIRONMENT_CAPTURE_START = "__T3CODE_PROJECT_ENVIRONMENT_START__";
+const ENVIRONMENT_CAPTURE_END = "__T3CODE_PROJECT_ENVIRONMENT_END__";
+const POSIX_CAPTURE_COMMAND = `printf '\0%s\0' '${ENVIRONMENT_CAPTURE_START}'; env -0; printf '%s\0' '${ENVIRONMENT_CAPTURE_END}'`;
+const WINDOWS_CAPTURE_COMMAND = [
+  `[Console]::Out.Write([char]0 + '${ENVIRONMENT_CAPTURE_START}' + [char]0)`,
+  "Get-ChildItem Env: | ForEach-Object { [Console]::Out.Write($_.Name + '=' + $_.Value + [char]0) }",
+  `[Console]::Out.Write('${ENVIRONMENT_CAPTURE_END}' + [char]0)`,
+].join("; ");
+
+function parseCapturedEnvironment(output: string): ProjectShellEnvironment | undefined {
+  const entries = output.split("\0");
+  const start = entries.indexOf(ENVIRONMENT_CAPTURE_START);
+  const end = entries.indexOf(ENVIRONMENT_CAPTURE_END, start + 1);
+  if (start === -1 || end === -1) return undefined;
+
+  const environment: ProjectShellEnvironment = {};
+  for (const entry of entries.slice(start + 1, end)) {
+    const separator = entry.indexOf("=");
+    if (separator <= 0) continue;
+    environment[entry.slice(0, separator)] = entry.slice(separator + 1);
+  }
+  return environment;
+}
+
+const captureShellEnvironment = (
+  shell: string,
+  args: ReadonlyArray<string>,
+  command: string,
+  cwd: string,
+  baseEnv: NodeJS.ProcessEnv,
+): Effect.Effect<ProjectShellEnvironment, unknown> =>
+  Effect.callback<ProjectShellEnvironment, unknown>((resume) => {
+    const child = NodeChildProcess.spawn(shell, args, {
+      cwd,
+      env: baseEnv,
+      stdio: ["pipe", "pipe", "pipe"],
+    });
+    const chunks: Buffer[] = [];
+    let outputBytes = 0;
+    let settled = false;
+    const finish = (effect: Effect.Effect<ProjectShellEnvironment, unknown>) => {
+      if (settled) return;
+      settled = true;
+      clearTimeout(timeout);
+      resume(effect);
+    };
+    const timeout = setTimeout(() => {
+      child.kill();
+      finish(Effect.fail(new Error("Project shell environment capture timed out")));
+    }, 10_000);
+
+    child.stdout.on("data", (chunk: Buffer) => {
+      outputBytes += chunk.byteLength;
+      if (outputBytes > 1024 * 1024) {
+        child.kill();
+        finish(Effect.fail(new Error("Project shell environment capture exceeded 1 MiB")));
+        return;
+      }
+      chunks.push(chunk);
+    });
+    child.on("error", (cause) => finish(Effect.fail(cause)));
+    child.on("close", () => {
+      const environment = parseCapturedEnvironment(Buffer.concat(chunks).toString("utf8"));
+      finish(
+        environment
+          ? Effect.succeed(environment)
+          : Effect.fail(new Error("Shell environment capture markers were missing")),
+      );
+    });
+    child.stdin.end(`${command}\nexit\n`);
+
+    return Effect.sync(() => {
+      clearTimeout(timeout);
+      child.kill();
+    });
+  });
+
+const exportProjectShellEnvironment = Effect.fn("exportProjectShellEnvironment")(function* (
+  cwd: string,
+  baseEnv: NodeJS.ProcessEnv,
+) {
+  const platform = yield* HostProcessPlatform;
+  const candidates =
+    platform === "win32"
+      ? (["pwsh.exe", "powershell.exe"] as const).map((shell) => ({
+          shell,
+          args: ["-NoLogo", "-NoExit", "-Command", "-"],
+          command: WINDOWS_CAPTURE_COMMAND,
+        }))
+      : listLoginShellCandidates(platform, baseEnv.SHELL).map((shell) => ({
+          shell,
+          args: ["-il"],
+          command: POSIX_CAPTURE_COMMAND,
+        }));
+
+  for (const candidate of candidates) {
+    const environment = yield* captureShellEnvironment(
+      candidate.shell,
+      candidate.args,
+      candidate.command,
+      cwd,
+      baseEnv,
+    ).pipe(Effect.catch(() => Effect.succeed(undefined)));
+    if (environment) return environment;
+  }
+
+  return yield* Effect.fail(new Error("No project shell environment could be captured"));
+});

 export function mergeProviderInstanceEnvironment(
   environment: ProviderInstanceEnvironment | undefined,
@@ -14,3 +131,25 @@ export function mergeProviderInstanceEnvironment(
   }
   return next;
 }
+
+export function makeProviderEnvironmentResolver(
+  environment: ProviderInstanceEnvironment | undefined,
+  baseEnv: NodeJS.ProcessEnv = process.env,
+  exportEnvironment: (
+    cwd: string,
+    baseEnv: NodeJS.ProcessEnv,
+  ) => Effect.Effect<ProjectShellEnvironment, unknown> = exportProjectShellEnvironment,
+): ProviderEnvironmentResolver {
+  return (cwd) =>
+    exportEnvironment(cwd, baseEnv).pipe(
+      Effect.map((exported) => {
+        return mergeProviderInstanceEnvironment(environment, exported);
+      }),
+      Effect.catch((cause) =>
+        Effect.logDebug("Could not load project environment from the user's shell", {
+          cwd,
+          cause: cause instanceof Error ? cause.name : "UnknownError",
+        }).pipe(Effect.as(mergeProviderInstanceEnvironment(environment, baseEnv))),
+      ),
+    );
+}
diff --git a/apps/server/src/provider/opencodeRuntime.ts b/apps/server/src/provider/opencodeRuntime.ts
index afd806e5666e96131cce8b44dbf8dc0aa110f9a3..32ec3340af8d544ba2befd457b3af51bcb99c471 100644
--- a/apps/server/src/provider/opencodeRuntime.ts
+++ b/apps/server/src/provider/opencodeRuntime.ts
@@ -659,6 +659,7 @@ const makeOpenCodeRuntime = Effect.gen(function* () {
       const child = yield* spawner
         .spawn(
           ChildProcess.make(spawnCommand.command, spawnCommand.args, {
+            ...(input.directory ? { cwd: input.directory } : {}),
             detached: hostPlatform !== "win32",
             shell: spawnCommand.shell,
             env: {
