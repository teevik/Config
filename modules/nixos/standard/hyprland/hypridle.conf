listener {
    timeout = 300
    on-timeout = brightnessctl s 50%-
    on-resume = brightnessctl s 50%+
}

listener {
    timeout = 600
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}
